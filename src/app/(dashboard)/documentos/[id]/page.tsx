import{ArrowLeft}from"lucide-react";import Link from"next/link";import{notFound}from"next/navigation";import{getClinicalDocument}from"@/app/(dashboard)/documentos/actions";import{DocumentActions}from"@/components/clinical-documents/document-actions";import{DocumentEditor}from"@/components/clinical-documents/document-editor";import{Badge}from"@/components/ui/badge";import{Button}from"@/components/ui/button";
export default async function DocumentPage({params}:{params:Promise<{id:string}>}){const{id}=await params;const doc=await getClinicalDocument(id);if(!doc)notFound();return <section className="space-y-6"><div className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><Button variant="ghost" size="sm" className="-ml-2" render={<Link href={`/consultas/${doc.appointment_id}/documentos`}/>}><ArrowLeft/>Documentos</Button><div className="mt-3 flex items-center gap-2"><h1 className="text-3xl font-semibold">{doc.title}</h1><Badge variant={doc.status==="cancelled"?"destructive":"secondary"}>{doc.status}</Badge></div><p className="mt-1 text-sm text-muted-foreground">Paciente: {doc.patient?.social_name||doc.patient?.full_name}</p></div><DocumentActions id={doc.id} status={doc.status}/></div><DocumentEditor document={doc}/></section>}
