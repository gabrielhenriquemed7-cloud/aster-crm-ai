"use client";
import { Loader2, Save } from "lucide-react";import{useState}from"react";import{toast}from"sonner";import{saveClinicSettings}from"@/app/(dashboard)/configuracoes/actions";import{Button}from"@/components/ui/button";import{Input}from"@/components/ui/input";
const fields:[string,string,string?][]=[["name","Nome fantasia"],["legal_name","Razão social"],["cnpj","CNPJ"],["cnes","CNES"],["logo_url","URL da logomarca"],["phone","Telefone"],["whatsapp","WhatsApp"],["email","E-mail","email"],["zip_code","CEP"],["address","Endereço"],["address_number","Número"],["address_complement","Complemento"],["neighborhood","Bairro"],["city","Cidade"],["state","UF"]];
export function ClinicSettingsForm({clinic}:{clinic:Record<string,unknown>}){const[values,setValues]=useState<Record<string,string>>(()=>Object.fromEntries(fields.map(([key])=>[key,String(clinic[key]??"")])));const[saving,setSaving]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setSaving(true);const r=await saveClinicSettings(values);setSaving(false);if(r.error)return toast.error(r.error);toast.success(r.success);}return <form onSubmit={submit} className="grid gap-4 rounded-xl border bg-card p-5 sm:grid-cols-2">{fields.map(([key,label,type])=><label key={key} className={key==="address"||key==="logo_url"?"sm:col-span-2":""}><span className="text-sm font-medium">{label}{key==="name"&&" *"}</span><Input className="mt-1" type={type??"text"} value={values[key]??""} onChange={e=>setValues(v=>({...v,[key]:e.target.value}))}/></label>)}<div className="sm:col-span-2"><Button disabled={saving}>{saving?<Loader2 className="animate-spin"/>:<Save/>}Salvar configurações</Button></div></form>}
