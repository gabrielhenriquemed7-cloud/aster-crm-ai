"use client";
import { useState } from "react";
import { toast } from "sonner";
import { UserPlus } from "lucide-react";
import { inviteClinicMember, updateClinicMember } from "@/app/(dashboard)/settings/team/actions";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { clinicRoleLabels, type Clinic, type ClinicMember, type ClinicRole } from "@/lib/clinics/types";
const roles = ["clinic_admin", "doctor", "secretary", "receptionist"] as const;
export function TeamManager({ clinic, members, error }: { clinic: Clinic | null; members: ClinicMember[]; error: string | null }) { const [email, setEmail] = useState(""); const [fullName, setFullName] = useState(""); const [role, setRole] = useState<(typeof roles)[number]>("receptionist"); const [saving, setSaving] = useState(false); async function invite(event: React.FormEvent) { event.preventDefault(); setSaving(true); const result = await inviteClinicMember({ email, full_name: fullName, role }); setSaving(false); if (result.error) return toast.error(result.error); toast.success(result.success); setEmail(""); setFullName(""); } if (error) return <Card className="shadow-none"><CardContent className="p-6 text-sm text-muted-foreground">{error}</CardContent></Card>; return <div className="grid gap-6 xl:grid-cols-[360px_1fr]"><Card className="shadow-none"><CardHeader><CardTitle>Convidar para {clinic?.name}</CardTitle></CardHeader><CardContent><form onSubmit={invite} className="space-y-3"><Input value={fullName} onChange={(event) => setFullName(event.target.value)} placeholder="Nome completo" required /><Input value={email} onChange={(event) => setEmail(event.target.value)} type="email" placeholder="E-mail" required /><select className="h-9 w-full rounded-md border bg-background px-3 text-sm" value={role} onChange={(event) => setRole(event.target.value as (typeof roles)[number])}>{roles.map((item) => <option key={item} value={item}>{clinicRoleLabels[item]}</option>)}</select><Button type="submit" disabled={saving} className="w-full"><UserPlus /> Enviar convite</Button></form></CardContent></Card><Card className="shadow-none"><CardHeader><CardTitle>Usuários vinculados</CardTitle></CardHeader><CardContent className="space-y-3">{members.map((member) => <div key={member.id} className="flex flex-col gap-3 rounded-lg border p-3 sm:flex-row sm:items-center sm:justify-between"><div><p className="text-sm font-medium">{member.profile?.full_name || member.profile?.email || "Usuário"}</p><p className="text-xs text-muted-foreground">{member.profile?.email} · {member.status}</p></div><div className="flex gap-2"><select className="h-8 rounded-md border bg-background px-2 text-xs" value={member.role} disabled={member.role === "platform_admin"} onChange={async (event) => { const result = await updateClinicMember(member.id, { role: event.target.value as ClinicRole }); if (result.error) toast.error(result.error); else toast.success(result.success); }}><option value={member.role}>{clinicRoleLabels[member.role]}</option>{roles.filter((item) => item !== member.role).map((item) => <option key={item} value={item}>{clinicRoleLabels[item]}</option>)}</select><Button size="sm" variant={member.status === "active" ? "outline" : "secondary"} onClick={async () => { const result = await updateClinicMember(member.id, { status: member.status === "active" ? "inactive" : "active" }); if (result.error) toast.error(result.error); else toast.success(result.success); }}>{member.status === "active" ? "Desativar" : "Ativar"}</Button></div></div>)}</CardContent></Card></div>; }
