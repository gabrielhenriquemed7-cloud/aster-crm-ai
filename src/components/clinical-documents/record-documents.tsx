"use client";import{FilePlus2,Files,Loader2}from"lucide-react";import Link from"next/link";import{useRouter}from"next/navigation";import{useState}from"react";import{toast}from"sonner";import{createClinicalDocument}from"@/app/(dashboard)/documentos/actions";import{Button}from"@/components/ui/button";import{Card,CardContent,CardHeader,CardTitle}from"@/components/ui/card";import{clinicalDocumentLabels,type ClinicalDocumentType}from"@/lib/clinical-documents/types";
const types:ClinicalDocumentType[]=["prescription","exam_request","medical_certificate","attendance_declaration","referral","patient_guidance"];
export function RecordDocuments({appointmentId,canCreate}:{appointmentId:string;canCreate:boolean}){const router=useRouter();const[loading,setLoading]=useState<ClinicalDocumentType|null>(null);async function create(type:ClinicalDocumentType){setLoading(type);const result=await createClinicalDocument(appointmentId,type);setLoading(null);if("error" in result)return toast.error(result.error);router.push(`/documentos/${result.id}`);}return <Card className="shadow-none"><CardHeader className="flex flex-row items-center justify-between"><CardTitle className="flex items-center gap-2"><Files className="text-primary"/>Documentos</CardTitle><Button size="sm" variant="outline" render={<Link href={`/consultas/${appointmentId}/documentos`}/>}>Ver todos</Button></CardHeader><CardContent className="flex flex-wrap gap-2">{types.map(type=><Button type="button" size="sm" variant="outline" key={type} disabled={!canCreate||Boolean(loading)} onClick={()=>create(type)}>{loading===type?<Loader2 className="animate-spin"/>:<FilePlus2/>}{clinicalDocumentLabels[type]}</Button>)}</CardContent></Card>}
